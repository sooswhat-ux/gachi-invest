# CLAUDE.md

대한민국 고위공직자 재산공개(전자관보)를 **종목(상장주식) 단위로 집계**하는 파이프라인.
전자관보 PDF를 긁어와 사람별 '상장주식' 내역을 파싱하고, 종목별 보유 공직자 수/총주수 순위와
시세·포트폴리오 비중을 담은 단일 `index.html`을 생성한다.

## 구성 파일

| 파일 | 역할 |
|------|------|
| `gwanbo_crawler.py` | 크롤러 + 파서 오케스트레이터 + 단일 HTML 생성기. CLI 진입점. |
| `holdings_parser.py` | 증권 문자열 파서 엔진(종목명/수량/증감/분류). 파이프라인의 심장. |
| `gwanbo_index.json` | `index` 결과: 관보 문서 목록 메타데이터. |
| `pdfs/` | `download` 결과: 개인별 재산공개 PDF (약 560개, 2020~2026). |
| `krx_map.json` | KRX 상장종목 `{정규화명: 종목코드}` 사전 캐시(국내/해외 분류용). |
| `prices.json` | `price` 결과: 종목별 다시점 종가/현재가/등락률(국내 원화, 해외 달러). |
| `holdings_domestic.csv` / `holdings_overseas.csv` | `parse` 결과: 국내/해외 종목 순위표. |
| `index.html` | `parse` 결과: **메인 페이지**(문구·자산합계·리드·큰손랭킹·갈아타기·해설, 정적 30KB). |
| `stocks.html` | `parse` 결과: **종목 랭킹 페이지**(순위표·검색·종목 모달·인물 모달). `#s=종목` 딥링크 지원. |
| `people.html` | `parse` 결과: **공직자 검색 페이지**(이름/소속 검색·자산구성 바·주식 포트폴리오). `?p=성명` 딥링크. |
| `verify_against_newstapa.py` | **검증 전용** 스크립트. 뉴스타파 데이터로 우리 파서 결과를 대조. |
| `뉴스타파 데이터/newstapa-jaesan-YYYY/` | 뉴스타파 배포 재산자료(엑셀/CSV). **배포 금지·검증용만**(아래 참고). |
| `about.html` / `privacy.html` / `contact.html` | 소개·개인정보처리방침·문의 정적 페이지(수동 관리, parse와 무관). |
| `pages_style.css` | 위 정적 페이지 공용 스타일(네이비 톤, index와 톤 일치). |

## CLI 명령 (`python3 gwanbo_crawler.py <cmd>`)

- `index` — `SearchRestApi.jsp`(POST)로 관보 문서 목록 수집 → `gwanbo_index.json`.
- `download [--all|--year=Y|--from=Y|--to=Y]` — 기본 2026 정기공개 PDF를 `pdfs/`에 저장.
- `krx` — KRX 전종목 사전 강제 재구축(`kind.krx` → `data.krx` → GitHub 폴백) → `krx_map.json`.
- `price [--top=N]` — 순위 상위 N종목 시세 수집(네이버 금융/국내, yfinance/해외) → `prices.json`.
- `parse` — PDF들을 파싱해 순위·상세·시계열 계산 → CSV 2종 + `index.html`.
- `all` — `index` → `download` → `parse` 순차 실행.

## `parse` 데이터 흐름 (핵심)

`gwanbo_crawler.py:cmd_parse()`:

1. `pdfs/*.pdf`를 순회, `pypdf`로 페이지 텍스트 추출.
2. **소속/직위/성명** 추출: `HEADER`(풀헤더 있을 때) / `NAMEONLY`(성명만) 정규식.
3. **`details_of(text)`** (모듈 레벨, `verify_against_newstapa.py`와 공유) — 페이지에서 `상장주식` 블록을 뽑아냄:
   - `LISTED_KW`로 `상장주식` 출현을 **각각** 찾고, 관계(본인/배우자/자녀)별 블록을 개별로 끊는다.
     (한 사람 표에 `본인 상장주식…`, `배우자 상장주식…`, `자녀 상장주식…`이 여러 행 나올 수 있음.)
   - 각 블록을 `BLOCK_END`(비상장주식/▶/채권/총계) 또는 다음 `상장주식` 시작 전까지로 제한.
   - `MONEY4`로 **그 블록 자신의** 금액 4개 컬럼(전년가액·증가·감소·현재가액)을 잘라 버림.
   - `증 가`→`증가`, 관계어 접두 제거, 다중 공백 축소.
   - → 관계별 상장주식 나열 문자열 `d`(공백 구분) 리스트 반환.
   - ⚠ 과거 버그: 첫 `상장주식`부터 다음 ▶/총계까지 통째로 잡아 MONEY4로 첫 금액에서 잘라
     본인분만 남고 배우자·자녀분이 통째로 버려졌음(집계 ~30% 누락). 관계별 개별 블록으로 수정됨.
4. `d`를 **`holdings_parser.parse_securities(d)`** 에 넘김 → `split_items`로 종목별 분리 후 종목별 `Holding` 산출.
5. 종목 `key = normalize(name)` 기준으로 최신연도 상세(`dom_detail`/`ovs_detail`)와 시계열(`ts`) 누적.
6. `rank()`로 (보유인원, 총주수) 정렬 → CSV 저장, `detail_payload`(상위 200 상세)·`ts_payload`(2년+ 시계열) 생성.
7. `build_html(...)`로 데이터를 JS 리터럴(`DOM/OVS/DDOM/DOVS/TS`)에 치환해 `index.html` 출력.

## `holdings_parser.py` 파서 규칙

- `split_items(s)` — 증권 나열 문자열을 종목별로 분리. 구분자는 원칙적으로 `쉼표+공백`이되
  천단위 쉼표(`1,860,000`)나 증감괄호 내부 쉼표(`197,972 감소`)에서는 자르지 않음.
  공백 소실·괄호 미닫힘 원문도 복구 분리(아래 "주의" 참고).
- `parse_item` → `Holding(name, qty, change, direction, sold_out, krx_code, category)`.
  - `CHANGE_RE`로 말미 `(N주 증가/감소)` 분리, `QTY_RE`로 말미 수량 분리(소수 주식 지원).
  - 미닫힘 괄호 꼬리(`메타플랫폼스(페이스`) 제거, `classify`로 국내상장/해외추정/미매칭 판정.
- `normalize(name)` — 우선주·한글음차(에스케이→SK)·해외표기(NVIDIACORP→엔비디아) 통일. **공백 전부 제거**.
- `classify` — `KRX_LISTED` 사전 매칭 우선(한/영 추측 아님). `_OVERSEAS`로 해외 대표명 통합.
- `0주` = 전량매도(`sold_out`) → 보유 집계 제외.

## 주의 / 함정

- **`normalize`가 공백을 모두 제거**하므로, `index.html`에 저장된 종목 key는 공백이 없다.
  반면 `split_items`에 실제로 들어가는 `d`(`details_of` 출력)는 **공백이 있다**.
  → 저장된 key를 그대로 `split_items`에 넣어 재현하면 실제 파이프라인과 다르게 동작할 수 있다.
  (닫히지 않은 `(`가 있는 항목이 앞에 오면 `depth`가 0으로 복귀 못 해 뒤 항목이 뭉치던 버그가
  이 차이 때문에 격리 테스트에서 드러나지 않았음 — `split_items` 복구 로직이 쉼표 뒤 공백을
  건너뛰고 판정하도록 수정됨.)
- `MONEY4`는 종목 나열 직후의 금액 컬럼을 자르는 용도라, 종목명에 숫자가 많으면 오절단 위험이 있음.
- `KRX_LISTED`는 캐시(`krx_map.json`) 기반 + 코드 내 `RECENT` 수동 보강. 신규상장주 누락 시 보강 필요.
- 시세는 상위 종목 위주(기본 top 200)만 수집. 해외는 `OVERSEAS_TICKER`에 매핑된 종목만.

## 종목명 표기 통합 (중복/오분류 방지)

관보는 **정식명칭·다양한 표기**를, KRX 사전은 **약칭**을 써서 매칭이 어긋난다. `holdings_parser.normalize()`가
다음 순서로 대표명으로 통일한다:

- `_KR_ALIAS`: 관보 정식명·변형 → KRX 약칭. 이게 없으면 국내주가 '해외·기타'로 빠진다.
  예) 중소기업은행→기업은행, 한화생명보험→한화생명, 신한금융지주회사→신한지주, 한국항공우주산업→한국항공우주,
  KB금융지주→KB금융, CJ씨지브이→CJCGV, 현대바이오사이언스→현대바이오, HD현대인프라코어/현대두산인프라코어→두산인프라코어,
  맥쿼리한국인프라투융자회사→맥쿼리인프라, KH필룩스→필룩스. (모두 KRX 사전 존재 확인 후 등록)
- `_TRANSLIT`: 한글 음차 → 영문 약칭(엘에스→LS, 에이치엠엠→HMM 등).
- `_OVERSEAS`/`_OVERSEAS_FOLD`: 해외종목 한글음차·영문티커·철자변형을 대표명으로 병합.
  예) NVIDIAORD→엔비디아, PALANTIRTECHINC/…CLAORD→팔란티어, 나이키B/NIKEINC→나이키(우선주 아님, 클래스B가 상장주),
  마이크로스트래티지→스트래티지(개명), 리커전파머슈티컬스→리커전파마슈티컬스(철자변형), TSMCADR→TSMC.
  마지막에 **뒤 괄호주석 제거 재시도**로 `메타플랫폼스(페이스북)→메타`, `TSMC(ADR)→TSMC`도 통합.
- **우선주는 병합 금지**(삼성전자우≠삼성전자, 미래에셋증권2우B는 별도). 알파벳A/알파벳C도 클래스가 달라 별도.

**웹 디자인**: 네이비 계열(`--accent:#1f3a5f`, `--accent-2:#2e527f`) 톤. `.wrap` 최대폭 920px,
`th/td`는 `white-space:nowrap`(헤더 세로 줄바꿈 방지), 테이블은 `.tblwrap`(overflow-x:auto)로 감싸
좁은 화면에서 가로 스크롤. `@media(max-width:700px)`로 모바일 대응. 모달 차트는 네이비 라인+영역
채움+그리드 스타일(`chartHTML`) — 그래프 확장 시 이 톤 유지. 소개/개인정보처리방침/문의는 정적 페이지로
푸터에서 링크(`pages_style.css` 공용). **주의**: index.html 스타일 수정은 `build_html` 템플릿에서 할 것
(index.html은 parse 때마다 재생성됨).

**종목 해설 자동 생성 (단일 소스 = 파이썬)**: 해설 문장은 `build_html` 안 파이썬 `narrative()`가 생성해
`NARR = {종목명: html}`로 임베드한다. **모달**(JS `narrativeOf`는 `NARR[nm]` 조회만), **검색용 정적 섹션**
(`.digest`, 탭별 상위 12종목을 실제 HTML 텍스트로), **리드 요약**(`.lead`)·**메타 설명/OG**(`__DESC__`)이
모두 이 한 소스를 쓴다. → 문구 바꾸려면 `narrative()`만 수정. parse만 다시 돌리면 전부 최신화.
문장 구성: 보유인원+순위 → 총주수 → 추세(2년+ 연속 증감이면 "N년 연속") → 신규매수/전량매도 →
소속 상위 2곳(+상위3기관 55%↑면 편중 코멘트) → 시세. 조사(은/는) 받침 자동, 비한글은 "은(는)".
호버 툴팁은 별도의 JS `miniTrend`(한 줄 추세)+`nnew/nout`(신규 N·매도 M)를 씀.
- **신규 매수/전량 매도**: 관보의 자체 증감 표기로 판정(재직 진입·퇴임 노이즈 없음). cmd_parse에서
  `증가분==보유량`이면 신규 매수(`newbuy`), `0주(감소)`면 전량 매도(`soldout`)로 최신연도 성명을 모아
  detail_payload에 `nnew`/`nout`(인원)과 보유자 `new` 플래그로 임베드. 모달 명단엔 신규 매수자에 `신규` 뱃지(`.nb`).
- **SEO**: 해설이 JS 전용이면 크롤링이 안 되므로 정적 `.digest` 섹션 + `<meta description>`/OG/Twitter 태그로
  실제 텍스트를 노출. 정적 페이지 3종에도 `<meta description>` 있음.

**사이트명/첫화면**: 사이트명 **"같이투자 : 가치있는 투자를 다같이"**(title/OG, h1 옆 `.slogan`),
메인 문구 "공직자는 어디에 투자했을까?"(`.tag`), 서브 문구 "공직자의 선택, 데이터로 읽다"(`.tagsub`).
검색창 위 "공직자 N명의 재산을 검색할 수 있습니다"(`.searchlabel`, N=len(ppx)), 검색창 아래
오류 가능성·제보 고지(`.notice` → contact.html). 정적 페이지 3종도 사이트명 "같이투자"로 통일.
people.html은 엔터 시 재검색+첫 결과 상세 자동 오픈.
서브라인의 공직자 수는 `__NPEOPLE__`(=인물 포트폴리오 인원)로 자동 치환. 정적 페이지 3종의 사이트명은
아직 "고위공직자 인기 종목" — 이름 확정 시 함께 갱신할 것.

**자산 대분류 파싱 + 큰손 랭킹**: `SUBTOT`(`▶ 분류(소계) …현재가액`)·`TOTALROW`(`총 계 종전…현재`)로
전 자산 분류(토지/건물/예금/증권/채권/가상자산/현금/채무 등, 천원 단위)를 최신연도 인물별로 수집
(`person_assets`). 소속은 `person_meta`(풀헤더, 정확) → `person_loose`(느슨한 추적) → `person_port`
순 폴백(`_org_of`). 산출물:
- 첫화면 자산 합계 카드 4종(`__A_ALL__/__A_SEC__/__A_EST__/__A_COIN__`, `fmt_krw`로 조/억 표기)
- `큰손 랭킹` 섹션(`.whales`, 파이썬 정적 생성 → SEO 노출): 주식/부동산/코인/현금 큰손 + 올해 가장
  불어난 재산(총계 현재-종전). 각 top5, 이름 클릭 → 인물 포트폴리오(`PP` 있는 인물만 `.pl`).
- `갈아타기` 박스(`.switchbox`): soldout×newbuy 교집합 상위 3쌍(예: 삼성전자→엔비디아 42명).
금액은 공직자윤리법상 '신고가액'이라 시가와 다름 — 화면에도 그 주석이 붙어 있음. 랭킹 명칭은 "큰손 시리즈"로 확정.
- **집중투자 카드**: 증권 신고액 1억↑ 인물의 (한 종목 수량×25년말 종가)/증권신고액 최대 비중, 표시는 100% 캡.
  신고가액≠시가라 상위가 100%에 몰림 — 변별 높이려면 sec 하한 상향 여지.

**멀티페이지 구조 (build_site)**: `build_site()`가 3페이지 dict 반환, cmd_parse가 각각 저장.
- `index.html`: 완전 정적(JSON 임베드 없음, 30KB) — 문구/합계/리드/navcards/큰손+집중투자/갈아타기/해설.
  큰손 이름 → `people.html?p=성명`, 해설 제목 → `stocks.html#s=종목` 링크.
- `stocks.html`: 기존 단일페이지의 순위표+모달 부분. `#s=종목` 해시 딥링크로 모달 자동 오픈(해외면 탭 전환).
- `people.html`: `ppx`(전 인물 1,835명: 소속·직위·자산분류·총계 증감·주식 포트폴리오) 임베드.
  이름/소속 검색(기본 총재산순 50명), 행 클릭 → 상세(자산 구성 바·전년 대비 증감·포트폴리오), `?p=` 딥링크.
  긴 자산분류명은 JS `CATLABEL`로 축약(차량·권리 등). 소수점 주식은 2자리 보존(0주 표시 방지).
- 공용 내비 `_nav()`/`NAV_CSS`(홈/종목 랭킹/공직자 검색/소개). 새 페이지 추가 시 items에 등록.
- **홈 첫 화면 구성**(순서): 내비 → 히어로(문구) → 자산 합계 카드 → **공직자 검색창**(GET →
  `people.html?q=검색어`, people 페이지가 `?q=` 읽어 자동 필터) → **종목 랭킹 2카드**(국내 네이비 /
  해외 틸, 각각 1–5↔6–10 로테이션) → **섹션 메뉴 칩**(`.secmenu`: #whales/#switch/#digest 앵커,
  smooth scroll) → 리드 → 큰손 랭킹 → 갈아타기 → 해설.
- **홈 로테이션**: `data-rot` 컨테이너의 `.rp` 패널들을 5초 간격 순환(홈 하단 인라인 script).
  종목 랭킹 국내/해외 각 카드(1–5↔6–10), 큰손 카드 6종(각 1–5/6–10위 2패널).
  전환은 CSS `@keyframes rin`(페이드+슬라이드업) + 행별 스태거(nth-of-type 딜레이) — PPT '나타나기' 느낌.
  `prefers-reduced-motion` 대응 포함. 외부 라이브러리 불필요.
- **디자인 시스템(홈/인물)**: base_css — 본문 15px, h1 30px, 섹션 h2 20px+좌측 색 바. 큰손 카드는
  `--wc/--wbg` 커스텀 속성으로 카테고리 색(주식 네이비·부동산 그린·코인 앰버·현금 틸·증가 레드·집중 퍼플),
  `.whead` 틴트 헤더 + `.rk` 순위칩 + `.wli` 3열 그리드(칩/이름·소속/금액 — 줄넘김 없음, ellipsis).
  자산 합계 카드는 상단 3px 색 보더로 구분.

## 수록 범위 (중요)

**정부·국회·대법원·선관위 4개 관할 모두 수록**(뉴스타파 대조: 정부 실질 100%, 국회 335명 정확 일치,
대법원 139명·선관위 25명). **헌재만 미포함**(헌재공보 별도 공개) — 홈 서브라인·about.html에 명시.
- **국회공보 수집**: `python3 gwanbo_crawler.py assembly` — assembly.go.kr 공보의 재산공개 호
  (`ASSEMBLY_ISSUES` 하드코딩: 발행일·호수·atchFileId, 2020~2026). 매년 3월 새 호가 나오면
  gongbo.do 목록(검색어 '재산공개')에서 atchFileId 확인해 목록에 추가.
- **국회공보 파싱 주의**: PDF 추출 시 표 셀 경계 공백이 소실됨('소속국회 직위국회의원성명안철수',
  금액 4컬럼이 '123,132,000011,346,000…'처럼 붙음). 대응: HEADER/NAMEONLY `\s*` 완화,
  `split_glued_nums()`(천단위 콤마 패턴으로 붙은 금액 복원), `SUBTOT2`/`TOTALROW2` 폴백,
  `MONEY_GLUED`(공백+10자리 이상 숫자덩어리에서 종목 나열 컷), person_sec은 자산소계 '증권' 폴백.
- **종목 나열 컷 규칙 4종**(details_of): `MONEY4`(공백 4컬럼·관보) / `MONEY_GLUED`(공백+10자리↑ 붙은
  금액·국회) / `MONEY_TAIL`('주/)' 뒤 공백+단독 금액·국회 신규퇴직호 1컬럼형; 숫자 뒤 '주'면 수량이라 제외) /
  `MONEY_STUCK`(')' 직후 무공백 금액). 국회공보 뭉텅이 전부 이 조합으로 해소됨(콤마 키 0건).
- **헌재공보(ccourt.go.kr)**: 새소식 게시판(cbIdx=1127) '공보' 카테고리 월간 발행,
  다운로드 `/common/board/Download.do?bcIdx=…&streFileNm=….pdf`. 2026년 재산공개는
  **제353호(2026-3-20) 481쪽 '헌법재판소공직자윤리위원회 제2026-3호(재산변동사항공개목록)'**에 있으나
  해당 부분이 **이미지 스캔**이라 pypdf 텍스트 추출 불가 → OCR 없이는 자동 수록 불가(13명, 보류).
- **다운로드 필터 주의**: `want()`의 정기 판별은 기관별 제목 표기가 제각각이라 폭넓게 허용
  (정부 '정기재산변동', 대법원 '재산등록(변동)사항공개', 선관위 '공개대상자 재산…공개',
  **수시 '재산공개목록(수시)'** — '재산공개' 포함이면 통과). 월례 수시공개는 `index`→`download`→`parse`로 반영.
- **수시 최초등록 공고 양식**: 금액이 '가액/비고' **1컬럼**(종전/증감 없음) → `SUBTOT2`/`TOTALROW2`가
  1토큰이면 그 값을 현재가액/총계로 사용. 최초등록형(_prev 없음)은 총계를 자산소계 합(−채무)과
  교차검증해 30%↑ 어긋나면 합산값으로 보정. ※순재산=자산−채무라 채무 큰 인물은 총계가 작게 나오는 게 정상
  (예: 황교익 자산 13.8억−채무 13.3억=총계 5,665만원 — 관보 원문 그대로).
- **대법원 관보 양식**: 헤더가 '소 속/직 위/성 명'처럼 띄어쓰기되고 이름도 '조 희 대'로 공백 포함 →
  HEADER/NAMEONLY는 `소\\s?속` 식 유연 매칭 + 이름 뒤 '(단위' 앵커, `person_name()`으로 공백 제거.
- 선관위 2026 공고 2건은 stored_organ_nm이 '감사2과'로 저장되어 파일명에 선관위 표기가 없음.

## 헤더 파싱 주의

`HEADER` 정규식은 소속이 **빈 경우**(대통령: "소속 직위 대통령 성명 이재명")도 매칭하도록 소속을
`(.{0,60}?)` 비탐욕으로 잡음(복수 기관 소속도 전체 캡처). 헤더 미매칭 파일에서 초기값(파일명)이
소속/직위로 새지 않도록 `_nofile()` 가드가 rec/person_loose 기록 시 적용됨.

**인물 포트폴리오**: `cmd_parse`가 `person_port`(성명→{소속,직위,증권소계,국내/해외 [종목,주수]})를 만들어
`__PP__`로 임베드. 종목 모달 명단의 이름(`.pl`) 클릭 → `openPerson()` 인물 화면(2단 목록, 증권 신고액 억원 표기),
종목행(`tr.prow`) 클릭 → `openStock()` 종목 모달, `← 돌아가기`(`.backlink`) 지원. 모달 이벤트는 `mod` 위임 1곳.
동명이인은 구분 안 됨(성명 키) — 알려진 한계. 순위표는 상위 100위까지만 렌더(임베드는 전체).
모달 폭 820px + `#mod td` 줄바꿈 허용으로 소속/직위/성명/보유주수 4컬럼이 한 화면에 들어감.

**정규화 추가 규칙**: `normalize()` 최상단에서 NFKC로 전각→반각 통일(`ＫＢ금융`→`KB금융`),
공백 제거는 `\s+`(NBSP 포함). PDF 추출문엔 전각 영문·특수 공백이 섞여 나오므로 필수.

**해외주식 티커 표기**: `OVERSEAS_TICKER`(대표명→티커, 현재 83종목)를 기준으로 정리하고,
`build_html`이 `{종목명:티커}` 맵(`__TK__`)을 HTML에 심어 해외 테이블·모달에 티커 배지(`.tk`)를 종목명 옆에
표시한다(예: 엔비디아 NVDA). 티커로도 검색 가능. `노보노디스크(ADR)`처럼 뒤 괄호주석이 붙은 이름은
괄호를 떼고도 티커를 조회한다. 신규 상위 해외종목은 `OVERSEAS_TICKER`에 티커를 추가하면 표기·검색·시세에 반영.

- KRX 목록에 빠진 종목은 `gwanbo_crawler.py`의 `RECENT`에 코드 수동 보강(네이버 금융 조회로 확인).
  예) 미래에셋증권2우B=00680K(우선주, 보통주와 별도), 그린광학=0015G0(코스닥).
- 우선주/종류주는 병합 금지·별도 종목: 현대차우(005385)/현대차2우B(005387)는 현대차(005380)와 다름.
  `normalize`가 `현대자동차→현대차`, `N우선주(신형)→N우B`로 KRX 약칭에 맞춘다.

**남은 한계**:
- `한화인더스트리얼솔루션즈`(7명): 네이버·KRX에 그 명칭의 상장종목이 없음(한화 계열은 한화솔루션·한화비전·한화엔진 등).
  현 시점 매칭 불가 → '해외·기타'에 남음. 실제 종목/코드 확인되면 `RECENT`에 추가.
- `항서제약`은 중국주(항서의약 600276, 상해), `Alphabet`(클래스 미표기)은 A/C 판별 불가 → 해외·기타 유지(정상).
- 표기 통합은 상위·빈출 종목 위주라 롱테일에는 미병합 중복이 남을 수 있음(`_OVERSEAS`/`_KR_ALIAS`에 계속 추가).
- 검증: 뉴스타파 대조 시 종목별 보유인원비 중앙값 ≈ 1.00 유지 확인.

## 검증(뉴스타파 데이터)

- `뉴스타파 데이터/` 아래 `newstapa-jaesan-2011~2025`는 뉴스타파가 배포한 공직자 재산자료다.
  **우리 웹/산출물에 직접 사용 금지 — 우리 파서 결과 검증용으로만 사용한다.**
- `python3 verify_against_newstapa.py [--news-year=2025] [--all] [--top=40]`:
  뉴스타파 `상장주식` 명세를 우리 파서로 파싱·집계해 우리 관보 파이프라인 결과와 대조.
- **기준연도 정합 주의**: 뉴스타파 `jaesan-Y` = 공개 Y년 = **(Y-1)년말 기준**. 우리 `file_year`(발행연도-1)와
  맞추면 **뉴스타파 Y ↔ 우리 기준연도 (Y-1)**. (우리 최신 기준 2025는 뉴스타파 2026 필요→없음.
  그래서 검증은 뉴스타파 2025 ↔ 우리 기준연도 2024로 함.)
- 뉴스타파 관할기관은 정부/국회/대법원/선관위/헌재 전부지만 우리는 정부/대법원/선관위만 수집 →
  기본 비교는 그 셋으로 필터(`--all`로 전체). 정합·필터 후 종목별 인원비 중앙값 ≈ 1.00 확인됨.

## 뉴스·공시 연동 (news)

`python3 gwanbo_crawler.py news` — `api_keys.json`의 네이버 뉴스 검색 API(제목·링크·날짜만,
본문/요약 전재 금지)와 DART 오픈API(국내 상위 종목 최근 공시)로 `news.json` 생성 → parse 재실행으로
종목 모달(`newsBox`)·인물 상세에 표시. 대상은 parse가 만드는 `news_targets.json`(상위 30×2 종목 +
큰손/집중투자 인물). 키가 없거나 플레이스홀더면 해당 부분 건너뜀(`_valid`). api_keys.json은 배포 제외 대상.

## 배포 (GitHub Pages)

- **라이브 URL**: https://sooswhat-ux.github.io/gachi-invest/ (저장소 sooswhat-ux/gachi-invest, main 브랜치 루트 서빙)
- **.gitignore 필수 제외**: `api_keys.json`(비밀키), `pdfs/`(원본 대용량), `뉴스타파 데이터/`(재배포 금지),
  `gwanbo_index.json`, `update.log`. 커밋 전 `git status`로 민감파일 미포함 확인 습관화.
- **자동 갱신**: `update.sh`(index→download→assembly→price→news→parse→변경 시 커밋·푸시)를
  launchd(`~/Library/LaunchAgents/kr.gachi-invest.update.plist`)가 **매일 08:00** 실행(맥 켜져 있을 때).
  로그: `update.log`, launchd 표준출력 `/tmp/gachi-update.{out,err}`.
  수동 갱신: `./update.sh`. 스케줄 해제: `launchctl unload ~/Library/LaunchAgents/kr.gachi-invest.update.plist`.

## 실행 순서

```
pip3 install requests pypdf yfinance
python3 gwanbo_crawler.py index
python3 gwanbo_crawler.py download      # 기본 2026 정기공개
python3 gwanbo_crawler.py parse         # → index.html
python3 gwanbo_crawler.py price         # 시세 보강 후 parse 재실행하면 화면 반영
```
